# Purpose

Prove that when a bridge restart replays Matrix events from an early global recovery cursor, per-room agent completion state prevents an event already completed by ACP from reaching the agent again.
